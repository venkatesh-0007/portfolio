<svg fill="none" viewBox="0 0 800 200" width="800" height="200" xmlns="http://www.w3.org/2000/svg">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        .container {
          width: 100%;
          height: 100%;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          background: #050505;
          color: #ffffff;
          font-family: "Syne", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
          position: relative;
          overflow: hidden;
          box-sizing: border-box;
          border: 1px solid rgba(255, 255, 255, 0.05);
          border-radius: 12px;
        }
        .glow {
          position: absolute;
          width: 400px;
          height: 400px;
          background: radial-gradient(circle, rgba(0, 229, 255, 0.15) 0%, rgba(0,0,0,0) 70%);
          border-radius: 50%;
          animation: float 8s ease-in-out infinite;
          opacity: 0.8;
          top: 50%;
          left: 50%;
          transform: translate(-50%, -50%);
        }
        .glow-secondary {
          position: absolute;
          width: 250px;
          height: 250px;
          background: radial-gradient(circle, rgba(200, 0, 255, 0.08) 0%, rgba(0,0,0,0) 70%);
          border-radius: 50%;
          animation: float-alt 6s ease-in-out infinite;
          top: 30%;
          left: 30%;
        }
        .grid {
          position: absolute;
          top: 0; left: 0; right: 0; bottom: 0;
          background-image: 
            linear-gradient(to right, rgba(255, 255, 255, 0.03) 1px, transparent 1px),
            linear-gradient(to bottom, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
          background-size: 30px 30px;
          opacity: 0.5;
        }
        .name {
          font-size: 46px;
          font-weight: 800;
          letter-spacing: -0.02em;
          margin: 0;
          z-index: 10;
          animation: slideUp 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
          opacity: 0;
          transform: translateY(20px);
        }
        .role {
          font-size: 18px;
          font-weight: 500;
          color: #a0a0a0;
          margin-top: 12px;
          z-index: 10;
          letter-spacing: 0.05em;
          animation: slideUp 1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
          opacity: 0;
          transform: translateY(20px);
        }
        .accent {
          color: #00e5ff;
        }
        @keyframes float {
          0%, 100% { transform: translate(-50%, -50%) scale(1); }
          50% { transform: translate(-50%, -50%) scale(1.2); }
        }
        @keyframes float-alt {
          0%, 100% { transform: translate(0, 0); }
          50% { transform: translate(30px, 30px); }
        }
        @keyframes slideUp {
          from { opacity: 0; transform: translateY(20px); }
          to { opacity: 1; transform: translateY(0); }
        }
      </style>
      <div class="container">
        <div class="grid"></div>
        <div class="glow"></div>
        <div class="glow-secondary"></div>
        <h1 class="name">Amudalapalli <span class="accent">Venkateswara Rao</span></h1>
        <p class="role">DATA SCIENCE STUDENT &amp; AI / ML ENGINEER</p>
      </div>
    </div>
  </foreignObject>
</svg>
